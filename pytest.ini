[pytest]
# 规范测试入口：根目录直接运行 `pytest` 等价于 `pytest tests`。
# 避免误收集 scripts/ 下的 test_*.py（如 scripts/test_elo_10matches.py，
# 那是手动运行入口而非单元测试）。
testpaths = tests
norecursedirs = scripts data venv .git .pytest_cache __pycache__ foot_repos
